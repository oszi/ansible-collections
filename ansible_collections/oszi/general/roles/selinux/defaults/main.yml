---
# Skip SELinux setup tasks?
selinux_disabled: "{{ ansible_facts.virtualization_type == 'container' or ansible_facts.os_family != 'RedHat' }}"

# Set SELinux mode to enforcing vs permissive?
selinux_is_enforcing: true
