---
# Leave remotes and apps empty by default!

# Disable flatpak?
# Install or uninstall flatpak when flatpak_method is system.
flatpak_disabled: false

# Flatpak installation method: system, or user in rootless-mode.
flatpak_method: "{{ 'system' if ansible_facts.user_uid | int == 0 else 'user' }}"

# Dict of remotes to install:
# {flathub: "https://flathub.org/repo/flathub.flatpakrepo"}
flatpak_remotes: {}

# Dict of apps to remotes to install:
# {org.chromium.Chromium: flathub}
flatpak_apps: {}

# List of apps to uninstall:
# - com.example.App
flatpak_blacklisted_apps: []

# List of global flatpak overrides:
# - "--nofilesystem=host"
# - "--nofilesystem=home"
flatpak_global_overrides: []

# Flatpak overrides in a dict of lists per app:
# {com.example.App: ["--nofilesystem=home", "--filesystem=/mnt/data:ro"]}
flatpak_app_overrides: {}

# Dict of aliases to bin exports to symlink in flatpak_exports_bin_path:
# {chromium: org.chromium.Chromium}
flatpak_exports_bin_aliases: {}

# List of obsolete filenames to remove from flatpak_exports_bin_path:
flatpak_exports_bin_aliases_absent: []
