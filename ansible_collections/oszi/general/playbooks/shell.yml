---
- name: Configure the shell environment
  hosts: all
  become: true
  gather_facts: false
  roles:
    - oszi.utils.bootstrap
    # locale (rootful) is not a dependency of shell...
    # shell supports rootless mode but this playbook is rootful.
    - oszi.general.locale
    - oszi.general.shell
