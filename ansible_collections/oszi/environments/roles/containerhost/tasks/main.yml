---
# First and foremost, this environment role depends on oszi.general.podman
- name: Containerhost role tasks
  tags: [containerhost]
  block:
    - name: Import toolbox-container tasks
      ansible.builtin.import_tasks:
        file: toolbox-container.yml
