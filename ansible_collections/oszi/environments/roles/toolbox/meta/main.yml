---
galaxy_info:
  description: "Setup a toolbox environment on hosts and in container images."
  author: "David O. (oszi.dev)"
  license: "BSD-3-Clause"
  min_ansible_version: "2.16"

  platforms:
    - name: Fedora
      versions: [all]
    - name: Debian
      versions: [all]
    - name: Ubuntu
      versions: [all]

  galaxy_tags:
    - environments
    - linux
    - toolbox

dependencies:
  - role: oszi.environments.baselinux
