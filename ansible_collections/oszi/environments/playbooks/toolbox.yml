---
- name: Deploy toolbox environments
  become: true
  gather_facts: false
  hosts: "{{ toolbox_hosts | default('toolbox*') }}"
  roles:
    - oszi.environments.toolbox

- name: Import playbook oszi.thirdparty.toolbox
  import_playbook: oszi.thirdparty.toolbox
