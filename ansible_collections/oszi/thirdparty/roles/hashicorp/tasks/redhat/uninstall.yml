---
- name: Uninstall HashiCorp RPM repository
  ansible.builtin.file:
    path: "{{ item }}"
    state: "absent"
  with_items:
    - "{{ hashicorp_rpm_gpgkey_path }}"
    - "{{ hashicorp_rpm_repo_path }}"
